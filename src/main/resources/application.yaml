spring:
  application:
    name: pruebadb
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/${DB_DATABASE}
    username: ${DB_USER}
    password: ${DB_PASS}
    #hikari:
     # maximum-pool-size: 100
      #minimum-idle: 100
db:
  pass: ${DB_PASS}
  user: ${DB_USER}

